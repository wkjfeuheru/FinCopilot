# 模块 3.1 config/ —— 配置中心

> **来源**：由《FinHarness —— 系统架构与模块详细设计》§3.1 拆分为独立文档；主文档保留总览与模块索引。
> **编号规则**：标题沿用主文档编号，正文中的交叉引用（如 "§3.4.1"、"见 §1.2"）同样沿用主文档编号，可在[主文档](../../FinHarness架构与模块详细设计.md)或对应模块文档中定位。
> **代码路径**：`src/finharness/config/settings.py`（门面：`Settings` 本体与跨节/启动校验）、`settings_models.py`（各配置节的嵌套模型与字段级校验）、`settings_loading.py`（读文件 / 深合并 / `FINH_` 白名单 / 路径解析）。公开名统一从 `settings.py` 重导出。

---

### 3.1 config/ —— 配置中心

**职责**：集中管理全部配置，唯一真实来源为仓库根目录 `settings.json` + 环境变量覆盖；加载期和启动期各校验一次。

```jsonc
// 根目录 settings.json 示例（键与 Pydantic 字段一一对应）
{
  "model": {
    "provider": "deepseek",          // 首选厂商
    "model_name": "deepseek-chat",
    "temperature": 0.1,
    "max_tokens": 4096,
    "thinking": { "enabled": true, "budget_tokens": 2048 }   // 支持厂商开关
  },
  "providers": {                     // 厂商表：anthropic 兼容 or openai 兼容
    "kimi":    { "kind": "anthropic_compat", "base_url": "https://api.moonshot.cn/anthropic/v1", "env_key": "MOONSHOT_API_KEY" },
    "glm":     { "kind": "anthropic_compat", "base_url": "https://open.bigmodel.cn/api/anthropic/v1", "env_key": "ZHIPU_API_KEY" },
    "deepseek":{ "kind": "openai_compat",    "base_url": "https://api.deepseek.com/v1",            "env_key": "DEEPSEEK_API_KEY" },
    "volcano": { "kind": "openai_compat",    "base_url": "https://ark.cn-beijing.volces.com/api/v3", "env_key": "ARK_API_KEY" },
    "qwen":    { "kind": "openai_compat",    "base_url": "https://dashscope.aliyuncs.com/compatible-mode/v1", "env_key": "DASHSCOPE_API_KEY" },
    "fake":    { "kind": "fake" }
  },
  "permission": { "default_mode": "default" },   // default | plan | auto
  "tools": {
    "timeout_default_s": 30,         // 数据接口默认超时
    "timeout_overrides": { "run_backtest": 300, "read_pdf": 60 },
    // 单条工具结果的 token 预算覆盖，键为工具名；未列出的工具用
    // 工具自身声明的 result_tokens，再退回 context.max_result_tokens。
    // 优先级与 timeout 对称：覆盖 > 工具声明 > 全局默认（§3.3.3）。
    "result_token_overrides": { "read_pdf": 8000 },
    "resident": [/* 24 个工具名，见 §3.4.1 表；留空则用内置默认 */],
    "lazy":     [/* 8 个工具名 */]
  },
  "data": {
    "adapter_order": ["fuyao", "akshare", "tushare", "baostock"],  // 顺序即优先级；见下方说明
    "tushare_token_env": "TUSHARE_TOKEN",
    "throttle_seconds": 1.0,         // 取数请求节流（同花顺与 akshare 共用）
    "cache_dir": "data_cache",
    "cache_ttl_days": { "quote": 1, "kline": 1, "indicators": 7, "financials": 365, "announcements": 7, "reports": 1, "macro": 7, "industry": 1, "web": 1, "dataset": 1 }
  },
  "context": {
    "max_turns": 30, "compaction_ratio": 0.8, "trim_rows": 20,
    "max_result_tokens": 1000, "max_tool_schema_tokens": 60,
    "compaction_result_tokens": 500, // 压缩转录稿中单条工具结果的 token 上限（§3.6.3）
    "short_mem_cap": 200,             // L2 事件环上限（§3.6.4）
    "recall_max_tokens": 400,         // L2 追问召回注入上限
    "summary_inject_max_tokens": 600, // 摘要层（分段历史）注入上限；原名 ltm_inject_max_tokens
    "max_loaded_rounds": 200,         // 会话重载时最多回放多少轮（更早的由摘要承载）
    "max_window_rounds": 40           // 窗口内轮次硬上限，超过即压缩（与 token 阈值并列）
  },
  "ltm": {                              // 跨对话长期记忆（§3.6.4「4.1」「4.2」）
    "distill_idle_s": 1800,             // 对话闲置多久后可被懒蒸馏（与 session_ttl_s 同量级）
    "distill_batch": 3,                 // 每轮扫描最多蒸馏多少个对话（成本有界）
    "distill_max_attempts": 3,          // 同一对话蒸馏失败重试上限，超出即放弃
    "recent_episodes": 5,               // 对话首轮被动注入的最近情节数
    "inject_max_tokens": 600,           // 【跨对话记忆】区块注入上限
    "recall_max_tokens": 400,           // 标的驱动的情节召回注入上限
    "retention_episodes": 500,          // 情节的独立保留上限（与源对话预算无关）
    "retention_days": 365,
    "distill_semantics": true,          // 语义记忆（事实/概念/偏好）随蒸馏产出；与情节共用一次调用
    "retention_facts": 300,             // 语义条目保留上限（偏好不受条数清理）
    "retention_facts_days": 730,
    "semantic_top_k": 5,                // 向量召回条数
    "semantic_inject_max_tokens": 400,  // 【相关知识】区块注入上限
    "embeddings": {                     // 远程 /embeddings（OpenAI 兼容）；留空即关闭语义召回
      "base_url": null, "env_key": null, "model_name": "", "timeout_s": 30.0
    },
    "vector_db": {                      // 向量库；记录本体在 SQLite，这里只做检索加速
      "kind": "qdrant", "url": null, "collection": "finharness_semantics",
      "api_key_env": null, "timeout_s": 10.0, "dim": 0   // dim=0 表示由首次嵌入推断
    }
  },
  "audit": { "log_path": "logs/audit.jsonl" },
  "server": {                             // v1.1：HTTP/SSE 服务层
    "host": "127.0.0.1", "port": 8000,
    "session_ttl_s": 1800,                // 会话 idle 回收
    "busy_timeout_s": 3600,               // busy 会话超过此时长视为被遗弃并可回收
    "user_cache_size": 64,                // 进程级用户级缓存（DataAccess）上限
    "confirm_ttl_s": 120,                 // write 确认等待上限
    "static_dir": "src/finharness/server/static",
    "allow_remote": false,                // true 才允许 0.0.0.0（须配 TLS 反代）
    "metrics_token": null                 // /metrics 抓取令牌；远程+启用 metrics 时必填
  },
  "paths": {
    // agent 可达目录（文件工具与 /v1/artifacts 开放其中的子树）
    "output_dir": "output",
    // 密钥与租户数据库所在的**不可达**目录（见 03.7 / 隔离方案 P0-3）
    "state_dir": "state",
    "memory_db": "state/memory.db",
    "auth_db": "state/users.db",
    "config_db": "state/config.db",
    "secret_key": "state/secret.key",
    "skills_dir": "skills"
  },
  "auth": {                              // 注册登录（03.13）
    "token_ttl_s": 3600, "min_password_len": 8,
    "secure_cookie": false,              // 经 HTTPS 对外暴露时必须置 true
    "allow_register": true,
    "login_max_attempts": 10, "login_window_s": 300,      // 登录限速（0 = 关闭）
    "register_max_attempts": 5, "register_window_s": 3600, // 注册限速
    "claim_legacy_on_first_register": false // 首个注册者是否继承存量数据（默认否）
  },
  "quota": {                             // 每租户用量预算（隔离方案 P0-6）
    "turns_per_window": 600, "window_s": 3600,  // 窗口滚动，默认防线
    "max_concurrent_streams": 0          // 0 = 不限制；开启前须确认 busy 会被及时释放
  },
  "search": {                            // 外部联网检索（web_search）与研报全文开关
    "kind": "tavily",
    "base_url": "https://api.tavily.com",
    "env_key": "TAVILY_API_KEY",         // 密钥只从环境变量读，不落配置文件
    "timeout_s": 30.0,
    "local_pdf_fallback": true           // 研报全文（with_text）是否允许本机抓取 PDF（见 03.4）
  },
  "fuyao": {                             // 同花顺金融数据（MCP，见 03.5）
    "enabled": true,
    "kind": "mcp",                       // 首期只实现 MCP 传输
    "base_url": "https://fuyao.aicubes.cn",
    "env_key": "HITHINK_FINANCE_API_KEY",
    "timeout_s": 30.0,
    "api_key": null,                     // 内联密钥（git 忽略的 settings.json 便利用法），优先于环境变量
    "proxy": null,                       // 留空回退到系统代理
    "catalog_ttl_s": 300.0               // 数据集目录（tools/list）的内存缓存时长
  }
}
```

> `search` 的写法与 `providers` 一致：`kind` 选实现、`env_key` 指定密钥变量。**未配置密钥不是加载错误**——工具在被调用时返回结构化"未配置"提示（研报工具不需要密钥）。`local_pdf_fallback`（环境变量 `FINH_SEARCH_LOCAL_PDF_FALLBACK`）默认开启；它是本项目唯一由本机直连文档地址的路径（用于 `get_research_reports` 的 `with_text=true` 全文抓取），带私网阻断，置 `false` 即禁用全文抓取。

> `fuyao` 与 `search` 同一口径：`env_key` 只存变量名，密钥由 `HITHINK_FINANCE_API_KEY`（或自定义变量）提供；也允许在未被跟踪的 `settings.json` 里内联 `api_key`。**未配置密钥同样不是加载错误**：适配器照常构造，调用时报"未配置同花顺密钥"（不可重试），编排器转给 akshare，因此未使用同花顺的部署不会被它拖住启动。`enabled=false` 关闭该数据源。

> **`adapter_order` 的顺序即优先级**，而非纯运维旋钮：编排取第一个不报错的适配器，因此把同花顺排在 akshare **之后**，就意味着 akshare 一旦答得上，同花顺永远不会被用到——那正是财报结构化字段与概念指数这类缺口一直填不上的原因。默认顺序 `fuyao → akshare → tushare → baostock`（`tushare` 与 `baostock` 目前未在运行时实例化，仅为配置预留）。同花顺的配额消耗由 `throttle_seconds` 统一节流。

- 实现：`pydantic-settings` 的 `Settings(BaseSettings)` 与冻结嵌套模型；全部字段采用严格类型，未知 JSON 字段、非法枚举和越界值均拒绝。深合并优先级为：环境变量 > `settings.json` > 代码内置默认。
- 默认 Provider：`deepseek`（`deepseek-chat`）；内置表包含 `deepseek`、`kimi`、`glm`、`volcano`、`qwen`、`fake`。`fake` 只允许显式选择，不作为错误降级目标。`volcano`（火山方舟）与 `qwen`（阿里云百炼 DashScope 兼容模式）均为 OpenAI 兼容端点。
- **预设接口不列出 `fake`**（2026-09 修订）：`GET /v1/config/presets` 与设置页的"供应商预设"下拉均过滤掉 fake 条目——它没有可填的 base_url/key，一键填入毫无内容。离线运行走 `model.provider: "fake"` 的显式选择，或在设置页"协议类型"下拉中选择"离线 Fake"；预设下拉另提供"自定义配置（手动填写）"入口。
- 路径：`data.cache_dir`、`audit.log_path`、`server.static_dir` 和 `paths.*` 的相对值均以 `settings.json` 所在目录为基准解析为绝对路径。加载配置不会创建目录。
- **状态与 agent 可达目录必须分离**：`paths.memory_db`／`auth_db`／`config_db`／`secret_key` 不得位于 `paths.output_dir` 或 `data.cache_dir` 之内，否则加载即失败（这是模型级校验，直接构造 `Settings` 的调用方也拦得住）。理由是文件工具与 `/v1/artifacts` 开放这两个目录的子树，把主密钥与租户数据库放在那里等于让"读一个缓存文件"与"读到全部租户的供应商 Key"之间只隔一次路径检查。详见《生产环境隔离与多租户加固方案》P0-3。
- **从旧布局升级（2026-09 起，所有既有部署都要做一次）**：状态文件的默认位置由 `data_cache/` 改为 `state/`。若沿用默认路径升级，应用启动时会检测到"旧目录里的账号在当前库中不存在"并**直接失败并给出搬迁命令**，而不是在一个空库上正常启动——后者表现为账号登不进去、历史为空，与全新安装无法区分。搬迁命令（默认只预览，`--apply` 才写入；**复制而非移动**，旧目录保留作备份）：

  ```bash
  python -m finharness.config.state_migration --settings settings.json          # 预览
  python -m finharness.config.state_migration --settings settings.json --apply  # 执行
  ```

  搬迁按"密钥先于配置库"的顺序进行，并在结束时真去解一条供应商配置来验证密钥配对；若目标位置已有**另一把**密钥，配置库不会被复制（避免搬过去一个解不开的库），报告会说明如何二选一。把 `paths.*` 指回旧目录**不是**可行方案——那会被上面的分离校验拒绝。
- 加载期校验：检查 JSON 结构、字段类型、Provider 端点、引用关系、数值范围和远程监听安全规则。`allow_remote=false` 时只允许 `127.0.0.1`、`::1` 或 `localhost`；远程监听只允许 `permission.default_mode=default`。
- 启动期校验：`Settings.validate_runtime()` 检查所选 Provider 的 API Key 和审计日志父目录可写性，但不访问网络、不创建目录。缺少 Key 时明确失败，不自动切换 Provider。
- 运行期只读：`Settings` 及每个嵌套配置模型均为 frozen，集合字段也转换为不可变容器；修改配置需要重启进程。

#### 环境变量契约

仅支持 `FINH_` 前缀和单下划线字段路径。双下划线或未知的 `FINH_` 变量会被拒绝，避免拼写错误被静默忽略。字符串直接填写；整数、浮点数和布尔值使用 JSON 标量；列表、对象必须使用 JSON。

常用示例：

```powershell
$env:FINH_MODEL_PROVIDER = "glm"
$env:FINH_MODEL_MODEL_NAME = "glm-4.5"
$env:FINH_MODEL_THINKING_ENABLED = "false"
$env:FINH_SERVER_PORT = "8123"
$env:FINH_DATA_ADAPTER_ORDER = '["fuyao","akshare"]'
$env:FINH_FUYAO_ENV_KEY = "HITHINK_FINANCE_API_KEY"
$env:FINH_TOOLS_TIMEOUT_OVERRIDES = '{"get_kline":45}'
```

支持覆盖的分组如下：

- `MODEL_*`：`PROVIDER`、`MODEL_NAME`、`TEMPERATURE`、`MAX_TOKENS`、`THINKING_ENABLED`、`THINKING_BUDGET_TOKENS`。
- `PROVIDERS`：用一个 JSON object 整体替换 Provider 表；内置 Provider 还可使用 `PROVIDERS_<DEEPSEEK|KIMI|GLM|VOLCANO|QWEN|FAKE>_<字段>` 覆盖。
- `PERMISSION_*`：`DEFAULT_MODE`。
- `TOOLS_*`：`TIMEOUT_DEFAULT_S`、`TIMEOUT_OVERRIDES`、`RESIDENT`、`LAZY`。
- `DATA_*`：`ADAPTER_ORDER`、`TUSHARE_TOKEN_ENV`、`THROTTLE_SECONDS`、`CACHE_DIR`、`CACHE_TTL_DAYS`。
- `FUYAO_*`（同花顺，见 03.5）：`ENABLED`、`KIND`、`BASE_URL`、`ENV_KEY`、`TIMEOUT_S`、`API_KEY`、`PROXY`、`CATALOG_TTL_S`。
- `CONTEXT_*`：与本节 JSON 示例中的 context 字段一一对应。
- `AUDIT_LOG_PATH`、`SERVER_*` 与 `PATHS_*`：与本节 JSON 示例中的对应字段一一对应。

Provider 的 API Key 本身不使用 `FINH_` 前缀；配置中的 `env_key` 只保存变量名，真实密钥仍由 `DEEPSEEK_API_KEY`、`MOONSHOT_API_KEY`、`ZHIPU_API_KEY` 或自定义变量提供。
